# Upstash Redis credentials (used in production / on Vercel).
# If both are unset, the app falls back to local JSON files under ./data — fine for local dev.
# Get these from the Upstash Redis integration in the Vercel Marketplace.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
